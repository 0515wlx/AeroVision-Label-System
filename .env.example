# AeroVision Label System - Environment Variables
# 复制此文件为 .env 并根据需要修改

# ==================== 目录配置 ====================
# 待标注图片目录
IMAGES_DIR=./images

# 已标注图片输出目录
LABELED_DIR=./labeled

# AI 模型文件目录
MODELS_DIR=./models

# ==================== 资源限制 ====================
# CPU 核心数限制 (docker-compose)
CPU_LIMIT=4.0

# 内存限制 (docker-compose)
MEMORY_LIMIT=8G

# CPU 推理线程数
CPU_THREADS=4

# ==================== 服务配置 ====================
# Flask 端口
FLASK_PORT=5000

# Flask 环境 (production/development)
FLASK_ENV=production

# 调试模式
FLASK_DEBUG=false

# ==================== OCR API 配置 ====================
# OCR API 服务地址（必须配置）
# 
# 选项 1: 访问宿主机上的 OCR API 服务（Docker Desktop 自动支持）
OCR_API_URL=http://host.docker.internal:8000/v2/models/ocr/infer
#
# 选项 2: 访问其他机器上的 OCR API 服务
# OCR_API_URL=http://192.168.1.100:8000/v2/models/ocr/infer
#
# 选项 3: 访问云端 OCR API 服务
# OCR_API_URL=https://your-ocr-api.example.com/v2/models/ocr/infer
#
# 注意: 
# - 确保 OCR API 服务已启动并可访问
# - 如果使用 host.docker.internal，确保 Docker Desktop 已启动
# - Linux 系统可能需要使用宿主机 IP 地址代替 host.docker.internal
