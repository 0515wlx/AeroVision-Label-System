# AeroVision Label System - GPU (CUDA 12.6) Version Dependencies
# 安装方式: pip install -r requirements.gpu.txt
# 驱动要求: 显卡驱动 ≥ 550.54.14

# 基础依赖
-r requirements.txt

# PyTorch GPU 版本 (CUDA 12.6, 阿里云镜像)
--find-links https://mirrors.aliyun.com/pytorch-wheels/cu126/
torch==2.9.1
torchvision==0.24.1

# PaddlePaddle GPU 版本 (CUDA 12.6, 官方源)
--extra-index-url https://www.paddlepaddle.org.cn/packages/stable/cu126/
paddlepaddle-gpu==3.2.0

# PaddleOCR 3.x (需要 PaddlePaddle >= 3.0)
paddleocr>=3.0.0
