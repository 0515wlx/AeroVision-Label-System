# AeroVision Label System - GPU (CUDA 12.6) Version Dependencies
# 安装方式: pip install -r requirements.gpu.txt
# 驱动要求: 显卡驱动 ≥ 550.54.14

# 基础依赖
-r requirements.txt

# PyTorch GPU 版本 (CUDA 12.6, 阿里云镜像)
--find-links https://mirrors.aliyun.com/pytorch-wheels/cu126/
torch==2.9.1
torchvision==0.24.1

# 注意: PaddlePaddle 和 PaddleOCR 已移除
# OCR 功能现在通过外部 API 调用实现 (http://localhost:8000/v2/models/ocr/infer)
# 如需使用本地 PaddleOCR，请参考旧版本配置
